# Locality-Sensitive Hashing — Approximate NN Search
Approximate nearest-neighbour search via Locality-Sensitive Hashing.
### ■ Research Highlights
- **Random Projection LSH**: Hashes high-dimensional vectors into $b$-bit buckets using random hyperplane pro- **Multi-Probe LSH**: Probes neighbouring buckets to increase recall without proportionally increasing the n- **Algorithms Focus**: Plots recall vs. query time trade-off for varying number of hash tables and bits, rep**Tech Stack:** Python, `NumPy`.
